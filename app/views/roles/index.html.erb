<h1>Roles</h1>

<h4>Roles de la aplicación. NO editar ni eliminar (preguntar a Seba y Nico)</h4>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Rol</th>
      <th>Descripción</th>
      <th>Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.name %></td>
        <td><%= role.description if role.description %></td>
        <td><%= link_to '<i class="fa fa-pencil"></i>   Editar'.html_safe, edit_role_path(role), :class => "btn btn-warning" %>
        <%= link_to '<i class="fa fa-times"></i>   Eliminar'.html_safe, role, :class => "btn btn-danger", method: :delete, data: { confirm: '¿Estás seguro? (Eliminar en caso de que sea estrictamente necesario, corrobar con Nico y Seba)' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '<i class="fa fa-check"></i>   Nuevo Rol'.html_safe, new_role_path, :class => "btn btn-primary" %>
