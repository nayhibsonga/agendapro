<%= form_for @status, :html => {:class => 'form-horizontal'} do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> no permitió que el estado sea creado:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, 'Nombre', :class => 'col-sm-4 control.label' %>
    <div class="col-sm-8">
      <%= f.text_field :name, :autofocus => true, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :description, 'Descripcion', :class => 'col-sm-4 control.label' %>
    <div class="col-sm-8">
      <%= f.text_area :description, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-4 col-sm-8">
      <%= submit_tag 'Guardar', :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
