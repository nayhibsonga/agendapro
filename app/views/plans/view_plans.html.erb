<section class="hgroup">
    <div class="container">
        <div class="row">
            <div class="col-sm-5">
                <h1>Planes</h1>
                <h3>Elige el plan que mejor se acomode a tus necesidades o <strong>contáctanos si necesitas uno a tu medida.</strong></h3>
            </div>
            <div class="col-sm-2 text-center">
                
            </div>
            <div class="col-sm-5">
                <h1>&nbsp;</h1>
                <h3 class="right-pricing">¡El <strong> primer mes </strong> es <strong>absolutamente gratis!</strong> Con sólo <strong> 2 clicks </strong> crea tu cuenta <strong>AgendaPro</strong></strong></h3>
            </div>
        </div>
    </div>
</section>
<div class="pricing_wrapper" style="background-image: url(<%= asset_path 'backgrounds/gray-background.png' %>);">
    <div class="container">
        <div class="row">
            <% @plans.each do |plan| %>
            <div class="col-sm-6 col-md-3">
                <div class="pricing_plan <%= 'special' if plan.special %>">
                    <h3><strong><%= plan.name %></strong></h3>
                    <hr class="pricing-hr">
                    <div class="the_price"><span>$</span><%= number_with_precision(plan.price, precision: 0, separator: '', delimiter: '.') %><small>/Mes<br>(+ IVA)</small></div>
                    <div class="the_offerings">
                        <p> <strong>¡Primer mes GRATIS!</strong><br /><strong><%= plan.locations %></strong> Local(es)<br /><strong><%= plan.service_providers %></strong> Staffs </p>
                        <br>
                    </div>
                </div>
            </div>
            <% end %>
        </div>
    </div>
</div>
<section>
    <div class="container">
        <div class="row text-center">
            <h1><%= link_to "¡Pruébalo Gratis Ahora!", new_user_registration_path(:plan => Plan.find_by_name("Trial").id, :company => true), :class => "btn btn-orange btn-lg" %></h1>
        </div>
    </div>
</section>

<% content_for :scripts do %>
<script>
    $(function() {
        $("#c_pricing").addClass("active")
        $("body").addClass("collapsing_header")
    });
</script>
<% end %>