<div class="container">
  <div class="row">
    <section class="hgroup">
      <h2>Minisitio</h2>
      <h2 class="section_header">Acá podrás definir si desea tener activado el minisitio, y si desea aparecer en el buscador.</h2>
      <div class="line-breadcrumb">
        <ul class="breadcrumb pull-right">
            <li><%= link_to "Resumen", dashboard_path %> </li>
            <li class="active">Minisitio</li>
        </ul>
    </div>
    </section>
  </div>
</div>

<div class="container">
  	<div class="row well">
		<%= form_for @company_setting, :html => {:class => 'form-horizontal'} do |f| %>
			<% if @company_setting.errors.any? %>
				<% content_for :scripts do %>
          <% message = '' %>
          <% @company_setting.errors.full_messages.each do |msg| %>
            <% message += '* ' + msg + '\n' %>
          <% end %>
          <script type="text/javascript">
            swal({
              title: "Error al guardar la configuración",
              text: "<%= message %>",
              type: "error"
            });
          </script>
        <% end %>
			<% end %>

			<div class="form-group">
				<%= f.label :activate_workflow, 'Habilitar Minisitio', :class => 'col-sm-3 control-label' %>
				<div class="col-sm-5">
					<%= f.check_box :activate_workflow %>
					<span class="help-block">Esta opcion permite a los clientes poder agendar a traves del sistema web, sin la necesidad de que lo esten llamando.</span>
				</div>
			</div>
			<div class="form-group">
				<%= f.label :activate_search, 'Aparecer en el Buscador', :class => 'col-sm-3 control-label' %>
				<div class="col-sm-5">
					<%= f.check_box :activate_search %>
					<span class="help-block">Esta opcion permite a los clientes poder encontrar el minisitio a traves del buscador.</span>
				</div>
			</div>
			<div class="form-group">
				<%= f.label :client_exclusive, 'Exclusivo para Clientes', :class => 'col-sm-3 control-label' %>
				<div class="col-sm-5">
					<%= f.check_box :client_exclusive %>
					<span class="help-block">Esta opcion limita que sólo puedan reservar clientes ingresados en la sección <%= link_to "Clientes", clients_path %>, usando el <%= (I18n.t('ci')).capitalize %>.</span>
				</div>
			</div>
			<!-- <div class="form-group">
				<div class="col-sm-offset-3 col-sm-4">
					<div class="checkbox">
						<label>
							<%= f.check_box :activate_workflow %> Habilitar Minisitio
						</label>
					</div>
					<span class="help-block">Esta opcion permite a los clientes poder agendar a traves del sistema web, sin la necesidad de que lo esten llamando.</span>
				</div>
			</div>
			<div class="form-group">
				<div class="col-sm-offset-3 col-sm-4">
					<div class="checkbox">
						<label>
							<%= f.check_box :activate_search %> Aparecer en el Buscador
						</label>
					</div>
					<span class="help-block">Esta opcion permite a los clientes poder encontrar el minisitio a traves del buscador.</span>
				</div>
			</div> -->
			<div class="form-group">
				<div class="col-sm-offset-3 col-sm-4">
					<%= f.submit 'Guardar', :class => 'btn btn-agendapro-claro' %>
					<%= link_to 'Cancelar', dashboard_path, :class => "btn btn-default" %>
				</div>
			</div>
		<% end %>
	</div>
</div>
