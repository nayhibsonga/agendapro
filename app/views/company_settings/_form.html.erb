<%= form_for @company_setting, :html => {:class => 'form-horizontal'} do |f| %>
  <% if @company_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company_setting.errors.count, "error") %> prohibited this company_setting from being saved:</h2>

      <ul>
      <% @company_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :company %>
  <div class="form-group">
    <%= f.label :email, 'Email', :class => 'col-sm-4 control.label' %>
    <div class="col-sm-8">
      <%= f.check_box :email %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :signature, 'Firma', :class => 'col-sm-4 control.label' %>
    <div class="col-sm-8">
      <%= f.text_area :signature, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-4 col-sm-8">
      <%= submit_tag 'Guardar', :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
