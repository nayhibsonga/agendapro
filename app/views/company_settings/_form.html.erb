<%= form_for @company_setting, :html => {:class => 'form-horizontal'} do |f| %>
	<% if @company_setting.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@company_setting.errors.count, "error") %> prohibited this company_setting from being saved:</h2>

			<ul>
			<% @company_setting.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>

	<%= f.hidden_field :company %>
	<!-- <div class="form-group">
		<%= f.label :email, 'Email', :class => 'col-sm-3 control-label' %>
		<div class="col-sm-4">
			<div class="checkbox">
				<label>
					<%= f.check_box :email %>
				</label>
			</div>
		</div>
	</div> -->
	<div class="form-group">
		<%= f.label :signature, 'Firma', :class => 'col-sm-3 control-label' %>
		<div class="col-sm-4">
			<%= f.text_area :signature, :class => 'form-control', :rows => "7" %>
		</div>
		<span class="help-block">Esta es la firma que irá en los emails que se le envien a los clientes.</span>
	</div>
	<div class="form-group">
		<div class="col-sm-offset-3 col-sm-4">
			<%= f.submit 'Guardar', :class => 'btn btn-agendapro-oscuro' %>
			<%= link_to 'Cancelar', dashboard_path, :class => "btn btn-default" %>    
		</div>
	</div>

<% end %>
