<%= form_for(@economic_sector) do |f| %>
  <% if @economic_sector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@economic_sector.errors.count, "error") %> prohibited this economic_sector from being saved:</h2>

      <ul>
      <% @economic_sector.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
