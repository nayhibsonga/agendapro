<h1>Usuarios registrados en tu compañía</h1>

<h4>A continuación se presentan los usuarios que tienen cuenta asociada a tu empresa.</h4>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Email</th>
      <th>Tipo</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.role.name %></td>
        <td><%= link_to '<i class="fa fa-pencil"></i> Editar'.html_safe, edit_admin_user_path(:id => user.id), :class => "btn btn-warning" %>
          <%= link_to '<i class="fa fa-trash-o"></i> Eliminar'.html_safe, admin_user_path(user), :class => "btn btn-danger",  method: :delete, data: { confirm: '¿Estás seguro de eliminar al usuario seleccionado?' } unless user.id == current_user.id %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to '<i class="fa fa-plus"></i> Nuevo Usuario'.html_safe, new_admin_user_path, :class => "btn btn-primary" %>
