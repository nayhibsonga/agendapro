<%= simple_form_for(@service_category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, :label => "Nombre" %>
    <%= f.association :company, :label => "Empresa" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
