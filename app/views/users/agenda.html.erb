
<div class="main">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="about-title">
                    BIENVENIDO A TU AGENDA
                </div>
            </div>
        </div>
    </div>
    <div class="agenda-section">
	    <div class="container">
	    	<div class="row">
		    	<div class="col-md-12">
			    	<div class="about-subtitle">
		                <% if @user.first_name.blank? || @user.last_name.blank? %>
							<%= @user.email %>
						<% else %>
							<%= @user.first_name + " " + @user.last_name %>
						<% end %>
		            </div>
	            </div>
            </div>
            <div class="row">
            	<div class="col-md-12" style="text-align: center;">
            		<% if !@user.phone.blank? %>
            			<%= image_tag "search/email.png", :size => "20x15" %>&nbsp;Email:&nbsp;<%= @user.email %>&nbsp;&nbsp;&nbsp;
            			<%= image_tag "telefono.png", :size => "15x15" %>&nbsp;Teléfono:&nbsp;<%= @user.phone %>
            		<% else %>
            			<%= image_tag "search/email.png", :size => "20x15" %>&nbsp;Email:&nbsp;<%= @user.email %>
            		<% end %>
            	</div>
            </div>
            <div class="row" style="margin-top: 25px;">
	            <div class="col-md-12" style="text-align: center;">
	            	Puedes editar tus datos <%= link_to 'aquí', edit_user_registration_path %>.
	            </div>
            </div>
	    </div>
    </div>

    <div class="container">
		<div class="row">
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="spa">
						<%= image_tag "search/1spa.png", :class => "img-responsive", size: "47x33" %>
						<span>Spa</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="peluquería">
						<%= image_tag "search/2peluqueria.png", :class => "img-responsive", size: "47x33" %>
						<span>Peluquería</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="maquillaje">
						<%= image_tag "search/3maquillaje.png", :class => "img-responsive", size: "47x33" %>
						<span>Maquillaje</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="centro de estética">
						<%= image_tag "search/4centrosdeestetica.png", :class => "img-responsive", size: "47x33" %>
						<span>Centros de estética</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="masaje">
						<%= image_tag "search/5masajes.png", :class => "img-responsive", size: "47x33" %>
						<span>Masajes</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="manicure">
						<%= image_tag "search/6manosypies.png", :class => "img-responsive", size: "47x33" %>
						<span>Manos y pies</span>
				</div>
			</div>
		</div>
		<div class="row" style="margin-top: 20px;">
			<div class="col-md-2 col-md-offset-2">
				<div class="search-btn btn-default sector_search" data-search="medicina alternativa">
						<%= image_tag "search/7medicinaalternativa.png", :class => "img-responsive", size: "47x33" %>
						<span>Medicina alternativa</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="bronceado">
						<%= image_tag "search/8bronceados.png", :class => "img-responsive", size: "47x33" %>
						<span>Bronceados</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="tratamiento facial">
						<%= image_tag "search/9tratamientosfaciales.png", :class => "img-responsive", size: "47x33" %>
						<span>Tratamientos faciales</span>
				</div>
			</div>
			<div class="col-md-2">
				<div class="search-btn btn-default sector_search" data-search="tratamiento corporal">
						<%= image_tag "search/10tratamientoscorporales.png", :class => "img-responsive", size: "47x33" %>
						<span>Tratamientos corporales</span>
				</div>
			</div>
		</div>
    </div>

    <div class="container">   	
		<div class="reservas-header">
			Reservas Activas
		</div>
    	<div class="panel-reservas">
    		<div class="campos campos-header">
    			<div class="campo">
    				Inicio
    			</div>
    			<div class="campo">
    				Empresa
    			</div>
    			<div class="campo">
    				Local
    			</div>
    			<div class="campo">
    				Servicio
    			</div>
    			<div class="campo">
    				Staff
    			</div>
    			<div class="campo">
    				Precio
    			</div>
    			<div class="campo">
    				Opciones
    			</div>
    		</div>
    		<div style="clear: both;"></div>
    		<% @activeBookings.each do |booking| %>
    		<hr />
    		<div class="campos booking">
    			<div class="campo">
    				<%= l booking.start, format: :short %>
    			</div>
    			<div class="campo">
    				<%= Company.find(booking.service_provider.company_id).name %>
    			</div>
    			<div class="campo">
    				<%= booking.location.name %>
    			</div>
    			<div class="campo">
    				<%= booking.service.name %>
    			</div>
    			<div class="campo">
    				<%= booking.service_provider.public_name %>
    			</div>
    			<div class="campo">
    				$<%= number_with_precision(booking.service.price, precision: 0, separator: '', delimiter: '.') %>
    			</div>
    			<div class="campo">
    				<%= link_to '<i class="fa fa-pencil"></i> Editar'.html_safe, booking_edit_url(:confirmation_code => booking.confirmation_code), :class => 'btn btn-warning btn-xs' %>
					<%= link_to '<i class="fa fa-trash-o"></i> Cancelar'.html_safe, booking_cancel_url(:confirmation_code => booking.confirmation_code), :class => 'btn btn-danger btn-xs' %>
    			</div>
    		</div>
    		<div style="clear: both;"></div>
    		<% end %>

    		<div style="clear: both;"></div>
    	</div>
    </div>

    <div class="container">
    	<div class="reservas-header">
			Últimas Reservas
		</div>
    	<div class="panel-reservas">
    		<div class="campos">
    			<div class="campo2">
    				Inicio
    			</div>
    			<div class="campo2">
    				Empresa
    			</div>
    			<div class="campo2">
    				Local
    			</div>
    			<div class="campo2">
    				Servicio
    			</div>
    			<div class="campo2">
    				Staff
    			</div>
    			<div class="campo2">
    				Estado
    			</div>
    		</div>
    		<div style="clear: both;"></div>
    		<% @lastBookings.each do |booking| %>
    		<hr />
    		<div class="campos booking">
    			<div class="campo2">
    				<%= l booking.start, format: :short %>
    			</div>
    			<div class="campo2">
    				<%= Company.find(booking.service_provider.company_id).name %>
    			</div>
    			<div class="campo2">
    				<%= booking.location.name %>
    			</div>
    			<div class="campo2">
    				<%= booking.service.name %>
    			</div>
    			<div class="campo2">
    				<%= booking.service_provider.public_name %>
    			</div>
    			<div class="campo2">
    				<%= booking.status.name %>
    			</div>
    		</div>
    		<div style="clear: both;"></div>							
			<% end %>
    		<div style="clear: both;"></div>
    	</div>
    </div>

</div>
<% content_for :scripts do %>
    <%= javascript_include_tag "Alert" %>
    <script type="text/javascript">
        myAlert = new Alert();

        var input2 = document.getElementById('inputLocalization2');
        var options = {
          types: ['geocode'],
          componentRestrictions: {country: 'cl'}
        };
        var geocoder = new google.maps.Geocoder();
        var autocomplete2 = new google.maps.places.Autocomplete(input2, options);
        var infowindow = new google.maps.InfoWindow();
        google.maps.event.addListener(autocomplete2, 'place_changed', function() {
            if (autocomplete2.getPlace().geometry) {
                $('#inputLocalization').val(autocomplete2.getPlace().formatted_address);
                $('#latitude').val(autocomplete2.getPlace().geometry.location.k);
                $('#longitude').val(autocomplete2.getPlace().geometry.location.D);
                document.cookie = "lat=" + escape(autocomplete2.getPlace().geometry.location.k);
                document.cookie = "lng=" + escape(autocomplete2.getPlace().geometry.location.D);
                document.cookie = "formatted_address=" + escape(autocomplete2.getPlace().formatted_address);
            }
        });
        $("#inputLocalization2").focusin(function () {
            $(document).bind("keypress.key2", function(e) {
                if (e.which == 13) {
                    e.preventDefault();
                    selectFirstResult();
                }
            });
        });
        $("#inputLocalization2").focusout(function () {
            if(!$(".pac-container").is(":focus") && !$(".pac-container").is(":visible")) {
                selectFirstResult();
            }
            $(document).unbind("keypress.key2");
        });
         
         function selectFirstResult() {
            infowindow.close();
            $(".pac-container").hide();
            var firstResult = $(".pac-container .pac-item:first").text();
            geocoder.geocode({"address":firstResult }, function(results, status) {
                if (status == google.maps.GeocoderStatus.OK) {
                    var lat = results[0].geometry.location.lat(),
                        lng = results[0].geometry.location.lng(),
                        placeName = results[0].address_components[0].long_name,
                        latlng = new google.maps.LatLng(lat, lng);
                    $('#latitude').val(lat);
                    $('#longitude').val(lng);
                    document.cookie = "lat=" + escape(lat);
                    document.cookie = "lng=" + escape(lng);
                    $("#inputLocalization").val(results[0].formatted_address);
                    $("#inputLocalization2").val(results[0].formatted_address);
                    document.cookie = "formatted_address=" + escape(results[0].formatted_address);
                    $('#inputSearch').focus();
                    $('#search-form').submit();
                }
                else {
                    $("#inputLocalization").val('');
                    $("#inputLocalization2").val('');
                }
            });
         }
        function geolocate() {
            if (navigator.geolocation) {
                $('.geolocate_btn').html('&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<%= image_tag "small-loader.gif", :alt => "Loader", :id => "small_loader_header", :class => "small-loader-header" %>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;');
                var positioned = false;
                navigator.geolocation.getCurrentPosition(
                    function(position) {
                        positioned = true;
                        $('#latitude').val(position.coords.latitude);
                        $('#longitude').val(position.coords.longitude);
                        document.cookie = "lat=" + escape(position.coords.latitude);
                        document.cookie = "lng=" + escape(position.coords.longitude);
                        var latlng = new google.maps.LatLng(position.coords.latitude, position.coords.longitude);
                        geocoder.geocode({'latLng': latlng}, function(results, status) {
                            if (status == google.maps.GeocoderStatus.OK) {
                              if (results[0]) {
                                $('#inputLocalization').val(results[0].formatted_address);
                                $('#inputLocalization2').val(results[0].formatted_address);
                                document.cookie = "formatted_address=" + escape(results[0].formatted_address);
                              } else {
                                $('#inputLocalization').val(position.coords.latitude+', '+position.coords.longitude);
                                $('#inputLocalization2').val(position.coords.latitude+', '+position.coords.longitude);
                                document.cookie = "formatted_address=" + escape(position.coords.latitude+', '+position.coords.longitude);
                              }
                            } else {
                                $('#inputLocalization').val(position.coords.latitude+', '+position.coords.longitude);
                                $('#inputLocalization2').val(position.coords.latitude+', '+position.coords.longitude);
                                document.cookie = "formatted_address=" + escape(position.coords.latitude+', '+position.coords.longitude);
                            }
                            $('#inputSearch').focus();
                            $('.geolocate_btn').html('<i class="fa fa-crosshairs"></i> Encuéntrame');
                            return false;
                        });
                    },
                    function (error) {
                        positioned = true;
                        if (error.code == 1) {
                            myAlert.showAlert(
                                '<h3>Hubo un error en el proceso de localización.</h3>' +
                                '<p>No hay permiso, por favor asegúrate permitir la geolicalización automática en tu explorador.</p>'
                            );
                        }
                        else if (error.code == 2) {
                            myAlert.showAlert(
                                '<h3>Hubo un error en el proceso de localización.</h3>' +
                                '<p>Posición no disponible, por favor inténtalo nuevamente o escribe tu ubicación.</p>'
                            );
                        }
                        else if (error.code == 3) {
                            myAlert.showAlert(
                                '<h3>Hubo un error en el proceso de localización.</h3>' +
                                '<p>La consulta tardó mucho, por favor inténtalo nuevamente o escribe tu ubicación.</p>'
                            );
                        }
                        else {
                            myAlert.showAlert(
                                '<h3>Hubo un error en el proceso de localización.</h3>' +
                                '<p>Error desconocido, por favor inténtalo nuevamente o escribe tu ubicación.</p>'
                            );
                        }
                        $('.geolocate_btn').html('<i class="fa fa-crosshairs"></i> Encuéntrame');
                    },
                    {timeout: 10000}
                );
                setTimeout(function () {
                    if (!positioned) {
                        myAlert.showAlert(
                          '<h3>Hubo un error en el proceso de localización.</h3>' +
                          '<p>No hubo respuesta, por favor asegúrate permitir la geolicalización automática en tu explorador.</p>'
                        );
                        $('.geolocate_btn').html('<i class="fa fa-crosshairs"></i> Encuéntrame');
                    }
                }, 15000);
            }
            else {
                myAlert.showAlert(
                  '<h3>Hubo un error en el proceso de localización.</h3>' +
                  '<p>Este explorador no permite geolocalización automática, por favor escribe tu ubicación.</p>'
                );
            }
            return;
        }
        $('.geolocate_btn').click(function() {
            geolocate();
        });
        $('.sector_search').click(function() {
            $('#inputSearch').val($(this).data('search'));
            if ($('#inputLocalization').val() != 0) {
                $('#search-form').submit();
            }
            else {
                $('#geolocation-modal').modal('show');
                $('#inputLocalization2').focus();
            }
        });
        $('#modal_search').click(function() {
            $('#search-form').submit();
        });
    </script>
    <% if @lat && @lng && @formatted_address %>
    <script type="text/javascript">
        $('#latitude').val(<%= @lat %>);
        $('#longitude').val(<%= @lng %>);
        $('#inputLocalization').val('<%= @formatted_address %>');
        $('#inputLocalization2').val('<%= @formatted_address %>');
    </script>
    <% end %>
<% end %>
