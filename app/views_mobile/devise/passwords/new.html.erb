<div class="row">
  <div class="col-xs-12">
    <div class="single-local">
      <div class="center-load">
        <p>RECUPERAR CONTRASEÑA</p>
      </div>
    </div>
    <div id="reservar" class="visible">
      <%= form_for resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :class => 'loginform' } do |f| %>
        <% if resource.errors.any? %>
          <div class="well">
            <h3>Errores</h3>
            <ul>
              <% resource.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <div class="form-group">
          <%= f.label :email, 'Email' %>
          <%= f.email_field :email, :required => true, :autofocus => true, :class => 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.submit 'Recuperar contraseña', :class => 'btn btn-agendapro-orange btn-lg btn-block' %>
          <%= link_to "Cancelar", :back, :class => 'btn btn-default btn-lg btn-block' %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="col-xs-12">
    <div class="well">
      <p> ¿No quieres seguir con nosotros?
      <%= link_to "Cancelar mi cuenta", registration_path(resource_name), :data => { :confirm => "¿Estás seguro que deseas cancelar tu cuenta?" }, :method => :delete %>
      </p>
    </div>
  </div>
</div>
